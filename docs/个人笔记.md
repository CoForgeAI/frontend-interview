# 个人笔记

<!-- 在这里记录你的内容 -->

## 测试服务器

### 前端代码位置

```
/root/fe/lynote-web
```

## 生产服务器

### strapi

- 服务器地址

```bash
cd /var/www/strapi-docker/strapi/

docker build  --build-arg NODE_ENV=production -t lynotestrapiapp:latest -f Dockerfile .

docker rm -f lynotestrapiapp && docker run -d --name lynotestrapiapp -p 8337:1337 lynotestrapiapp:latest
```
